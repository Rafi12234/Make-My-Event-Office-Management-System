# Frontend connection

1. Copy `.env.example` to `.env.local`.
2. Start the Node/Express API on port 5000.
3. Start this frontend with `npm run dev`.

The updated `src/services/managementStorage.js` now uses the API instead of browser workspace storage.
